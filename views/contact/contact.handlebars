<h1 class='mb-4'>My Contact List</h1>
<a href='/contact/add' class='btn btn-primary mb-3'>New contact</a>

{{#if contacts.length}}
	<table class='table table-bordered'>
		<thead class='table-light'>
			<tr>
				<th>Name</th>
				<th>Email</th>
				<th>mobile</th>
				<th>Action</th>
			</tr>
		</thead>
		<tbody>
			{{#each contacts}}
				<tr>
					<td>{{this.name}}</td>
					<td>{{this.email}}</td>
					<td>{{this.mobile}}</td>
					<td>
						<a
							href='/contact/edit/{{this._id}}'
							class='btn btn-warning btn-sm'
						>Edit</a>
						<form
							action='/contact/delete/{{this._id}}'
							class='d-inline-block m-0'
						>
							<button
								type='submit'
								class='btn btn-danger btn-sm'
							>Delete</button>
						</form>
					</td>
				</tr>
			{{/each}}
		</tbody>
	</table>
{{else}}
	<div class='alert alert-warning text-center' role='alert'>
		<h2>Contacts not found</h2>
	</div>
{{/if}}